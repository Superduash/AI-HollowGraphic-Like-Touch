# Holographic Touch — AI Agent Rules
# This file is read automatically by VS Code Copilot/Codex.

## GOLDEN RULE
Change ONLY the lines described in the task. Do NOT:
- rename variables, methods, classes, or parameters
- remove code that is not explicitly mentioned
- change threshold values not explicitly mentioned
- add new imports unless explicitly told to
- "clean up", "refactor", or "simplify" anything
- change any method signature or return type

## OUTPUT FORMAT
- Always output the COMPLETE file — never use "# ... existing code ..."
- Copy every unchanged method CHARACTER FOR CHARACTER
- If unsure whether to change something: DON'T

## FROZEN VALUES (never change without explicit instruction)
# src/tuning.py:
GESTURE_CONFIRM_HOLD_S = 0.04
GESTURE_ACTION_COOLDOWN_S = 0.18
GESTURE_DRAG_ACTIVATE_S = 0.45
GESTURE_DOUBLE_CLICK_WINDOW_S = 0.45
GESTURE_RIGHT_CLICK_HOLD_S = 0.25
GESTURE_SCROLL_DIR_SWITCH_COOLDOWN_S = 0.25
MOUSE_WORKER_HZ = 240.0

# src/gesture_detector.py __init__:
self._pinch_enter = 0.22
self._pinch_exit = 0.36
self._scroll_step_limit = 8
self._scroll_gain = 1.0

# src/gesture_detector.py per-action cooldowns:
LEFT_CLICK: 0.25, RIGHT_CLICK: 0.50, DOUBLE_CLICK: 0.50

# src/settings_store.py DEFAULTS:
"pinch_sensitivity": 0.22
"pinch_exit_sensitivity": 0.38
"confirm_hold_s": 0.03

# src/hand_tracker.py Hands config:
max_num_hands = 2
min_detection_confidence = 0.55
min_tracking_confidence = 0.45

## FROZEN INTERFACES
- HandTracker.detect() returns (hands_dict, hand_protos_list, is_grace)
- GestureDetector.detect_dual(hands_dict, is_grace, cursor_label) returns GestureResult
- GestureDetector.detect(hand_data, is_grace_frame) returns GestureResult
- CursorMapper.map_point(cam_x, cam_y) returns (int, int)

## FEATURES THAT MUST NOT BE REMOVED
- Dual-hand mode, single-hand mode, auto mode switching
- Cursor freeze during clicks (self._freeze_on)
- Grace frame handling
- Right-click hold timer
- EMA-smoothed pinch ratios (self._li_ema, _ri_ema, _pm_ema)
- Scroll accumulator with direction-switch cooldown
- Drag progress arc, status overlay, system tray, Ctrl+Shift+H hotkey
- All GestureType enum values
